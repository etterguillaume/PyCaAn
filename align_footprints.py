#%% Imports
import numpy as np
import h5py
import matplotlib.pyplot as plt

#%% Create folder that will store results

#%% For each mouse, list sessions, initialize identity matrix 

#%% Open all corrproj (meanProj?)
# For each session pair, compute affine transform values

#%% Open all SFPs
# For each pair of sessions, apply transform, then compute centroid_dist, spatial corr
# If pass threshold, fill identity matrix

#%% Save results in folder

#%% Plot results



